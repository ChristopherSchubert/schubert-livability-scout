# Retired prototype mockups

These are **old, static design prototypes** — superseded HTML mockups moved out
of the served `public/` web root on 2026-06-22 (issue #75) so they are no longer
reachable on the production domain. They are kept here only as design history.

**Do not wire these to anything.** If you want one back as a live surface, move
it deliberately and document why.

## What stayed live (intentionally NOT retired)

One prototype is a real, API-wired surface and remains under `public/`:

- `public/city-detail-redesign.html` — design surface backed by `/api/mockup-data`.

## What's archived here

- `evidence-mockup.html`, `trip-planner-lines-demo.html`,
  `trip-planner-swimlanes-mockup.html` — superseded one-off explorations.
- `mockups/trip-*.html` — the standalone trip-planner screen iterations, plus
  `trip-walkthrough.html`, the canonical trip-planning deck. It had been kept as
  Janice's feedback surface (↔ `/api/walkthrough-feedback`); retired 2026-06-22
  once that feedback was incorporated. Preserved here as the design source of
  truth. NB: `/api/walkthrough-feedback` is now unused — a cleanup candidate.
